programa {
    cadena saludo = "Hola";
    cadena nombre = " mundo";
    cadena msg = saludo + nombre;
    imprimir(msg);

    entero i = 0;
    mientras (i < 3) {
        imprimir(i);
        i = i + 1;
    }
}
