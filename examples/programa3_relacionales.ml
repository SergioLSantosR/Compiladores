// Prueba: operadores relacionales
program {
  int x;
  int y;
  bool r;

  x = 10;
  y = 20;
  r = x < y;       // true
  print(r);
  r = x == y;      // false
  print(r);
  r = x != y;      // true
  print(r);
  r = y >= x;      // true
  print(r);
}
